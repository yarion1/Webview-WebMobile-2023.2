import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Webview-WebMobile-2023.2',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
